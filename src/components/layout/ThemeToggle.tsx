// ============================================================
// Theme Toggle — Disabled (dark-only system)
// Kept as stub so existing imports don't break
// ============================================================

export default function ThemeToggle() {
  return null;
}